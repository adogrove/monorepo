The `@adogrove/adonis-cap` package is available on [npm](https://www.npmjs.com/package/@adogrove/adonis-cap).
You can install it using the following ace command to automagically configure it:
```sh
node ace add @adogrove/adonis-cap
```

Alternatively, you can install it manually.
<!-- automd:installGroup -->

::: code-group
```sh [npm]
npm install @adogrove/adonis-cap
node ace configure @adogrove/adonis-cap
```
```sh [yarn]
yarn add @adogrove/adonis-cap
node ace configure @adogrove/adonis-cap
```
```sh [pnpm]
pnpm add @adogrove/adonis-cap
node ace configure @adogrove/adonis-cap
```
  :::

<!-- /automd -->